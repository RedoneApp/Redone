{
  "kind": "collectionType",
  "collectionName": "subtags",
  "info": {
    "name": "Subtag"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "Title": {
      "type": "string",
      "required": true
    },
    "tasks": {
      "collection": "task",
      "via": "subtags",
      "dominant": true
    },
    "tag": {
      "model": "tag",
      "via": "subtags"
    }
  }
}
